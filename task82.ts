// This function counts how many characters are in a string
function stringLength(str: string): number {
    return str.length; // Returns the number of characters in the string
}


console.log(stringLength("Baki")); 
